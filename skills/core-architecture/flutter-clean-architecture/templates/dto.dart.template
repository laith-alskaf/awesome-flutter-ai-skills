// Data Transfer Object (DTO) Template
// Place in: lib/features/<feature_name>/data/models/<entity_name>_dto.dart
// Rule: JsonSerializable annotations live here, never in Domain Entities!

import 'package:freezed_annotation/freezed_annotation.dart';

part '{{entity_name}}_dto.g.dart';

@JsonSerializable()
class {{EntityName}}Dto {
  const {{EntityName}}Dto({
    required this.id,
    required this.name,
    @JsonKey(name: 'created_at') required this.createdAt,
  });

  final String id;
  final String name;
  final String createdAt;

  factory {{EntityName}}Dto.fromJson(Map<String, dynamic> json) =>
      _${{EntityName}}DtoFromJson(json);

  Map<String, dynamic> toJson() => _${{EntityName}}DtoToJson(this);

  factory {{EntityName}}Dto.empty() => const {{EntityName}}Dto(
        id: '',
        name: '',
        createdAt: '',
      );
}
